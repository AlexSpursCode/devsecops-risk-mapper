# UI

Production dashboard (static frontend served by FastAPI) for:

- release risk KPIs
- SAMM/CISA compliance coverage bars
- findings table
- service threat/dataflow graph visualization
- async job status inspection

Files:

- `index.html`: app shell and panels
- `styles.css`: visual system and responsive layout
- `main.js`: API client, state loading, graph rendering
